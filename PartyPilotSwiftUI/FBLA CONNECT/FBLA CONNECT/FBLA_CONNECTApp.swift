import SwiftUI

/// App entry point.
/// SwiftUI starts here and loads the root view (`ContentView`).
@main
struct FBLA_CONNECTApp: App {
    var body: some Scene {
        WindowGroup {
            // Main app container with bottom tabs.
            ContentView()
        }
    }
}
